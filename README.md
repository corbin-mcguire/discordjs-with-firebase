# discordjs-with-firebase
## Running locally
```bash
$ npm install
$ npm run serve
```

## Running with Docker
```bash
$ docker build . -t yourContainerName
$ docker run -p 49160:8080 -d yourContainerName
```
